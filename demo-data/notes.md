# Sample notes

These are sample notes for the pagefault demo.

## Ideas

- Try the /api/search endpoint with different queries
- Call /api/get_context to load the "demo" context
- Inspect audit output on stdout when running with `make run`
